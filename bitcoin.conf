# bitcoin.conf

# Prune blockchain to ~10-15GB
prune=1000

# Allow RPC calls (optional, for your apps)
server=1
rpcbind=0.0.0.0
rpcallowip=0.0.0.0/0
rpcuser=bitcoinuser
rpcpassword=supersecurepassword

# Enable P2P connections
listen=1
txindex=0

# Optional logging
logtimestamps=1
