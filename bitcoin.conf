# Pruned blockchain (~10–15GB)
prune=1000

# Enable server mode
server=1

# RPC configuration
rpcbind=0.0.0.0
rpcallowip=0.0.0.0/0
rpcuser=bitcoinuser
rpcpassword=supersecurepassword

# Enable P2P connections
listen=1
txindex=0

# Optional logging
logtimestamps=1
